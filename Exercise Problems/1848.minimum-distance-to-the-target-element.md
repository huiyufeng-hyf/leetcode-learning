https://leetcode.cn/problems/minimum-distance-to-the-target-element/description/ 

- Solution by simulation
```
public class Solution {
    public int GetMinDistance(int[] nums, int target, int start) {
        if (nums[start] == target)
            return 0;

        var minValue = nums.Length;
        for (var i = 0; i < nums.Length; i++)
        {
            if (nums[i] == target)
                minValue = Math.Min(Math.Abs(i - start), minValue);
        }
        return minValue;
    }
}
```
**Python**
```
class Solution:
    def getMinDistance(self, nums: List[int], target: int, start: int) -> int:
        if nums[start] == target:
            return 0
        
        min_val = len(nums)
        for i in range(0, len(nums)):
            if nums[i] == target:
                min_val = min(abs(i - start), min_val)
        return min_val
        
```

---

- Recommendation by simulation in Python
```
class Solution:
    def getMinDistance(self, nums: List[int], target: int, start: int) -> int:
        res = len(nums)
        for i, num in enumerate(nums):
            if num == target:
                res = min(res, abs(i - start))
        return res
        
```
