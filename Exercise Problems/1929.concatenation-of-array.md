**https://leetcode.cn/problems/concatenation-of-array/description/**

- Solution by simulation
```
public class Solution {
    public int[] GetConcatenation(int[] nums) {
        var n = nums.Length;
        var ans = new int[2 * n];
        for (var i = 0; i < n; i++)
        {
            ans[i] = ans[i + n] = nums[i];
        }
        return ans;
    }
}
```
**Python**
```
class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        n = len(nums)
        ans = [0] * (2 * n)
        for i in range(n):
            ans[i] = ans[i + n] = nums[i]
        return ans
```

---

- Solution by using syntax in Python
```
class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        return nums + nums
```

---

- Recommendation buy concatenation in Python
```
class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        nums.extend(nums)
        return nums
```
